spring:
  profiles: test

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""      # 빈 문자열로 명시

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
